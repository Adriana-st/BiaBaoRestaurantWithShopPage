
<style>
  body {
    background: url('images/baozi.png') no-repeat center center/cover;
    background-attachment: fixed;
    background-size: cover;
    min-height: 100vh; /* Full viewport height */
    display: flex;
    flex-direction: column;
  }

  main {
    flex: 1; /* Pushes footer to the bottom */
    display: flex;
    justify-content: center;
    align-items: center;
    padding-top: 160px;
    padding-bottom: 80px;
  }

  footer {
    margin-top: auto;
  }

  .thankyou-card {
    width: 100%;
    max-width: 800px; /* Larger card on bigger screens */
  }

  @media (max-width: 768px) {
    main {
      padding-top: 120px; /* Slightly reduced for smaller screens */
      padding-bottom: 60px;
    }
    .thankyou-card {
      max-width: 90%;
    }
  }
</style>

<main class="container my-5 py-5 mt-5">
  <div class="card thankyou-card mx-auto shadow-lg border-0">
    <div class="card-header bg-danger text-white text-center py-4">
      <h2 class="mb-0 fw-bold">Thank You, {{name}}!</h2>
    </div>
    <div class="card-body text-center p-5 bg-light">
      <p class="lead">We've received your message and will get back to you at:</p>
      <p class="fw-semibold text-danger">{{email}}</p>
      <hr class="my-4">
      <h5 class="text-secondary mb-3">Your Message</h5>
      <p class="fst-italic text-dark">{{message}}</p>
      <hr class="my-4">
      <a href="/" class="btn btn-danger btn-lg px-5 mt-3 shadow-sm">Return to Home</a>
    </div>
  </div>
</main>
